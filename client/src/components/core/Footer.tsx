
const Footer = () => {
  return (
      <footer className="max-w-6xl mx-auto mt-6 text-xs text-gray-500">
        Tip: Use small prompts like "3-bullet summary" for crisp results. Integrate OCR on upload for better summaries of images/PDFs.
      </footer>
  )
}

export default Footer